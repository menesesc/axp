# Database connection string
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# 
# Para Supabase, obtené esta URL desde:
# Supabase Dashboard > Project Settings > Database > Connection String (URI)
# 
# IMPORTANTE: Usá la connection string en modo "Session" para development
# y "Transaction" para production con connection pooling

DATABASE_URL="postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres?schema=public"

# Alternativa con connection pooling (para producción):
# DATABASE_URL="postgresql://postgres:your-password@db.your-project.supabase.co:6543/postgres?pgbouncer=true&schema=public"
